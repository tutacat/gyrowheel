## Memory
* Doesn't want testing to be mentioned if there was no testing done.
* Do not use any Prettier or similar style checkers.
* Self-closing tags are not part of HTML spec.
* Project issues can be found using `gh issue ...` for example `gh issue list` for open issues, -s closed, or can `gh issue view 123` to see more details on a specific issue.
* `gh [command] --usage`, you can also use `gh [command] --help`.
